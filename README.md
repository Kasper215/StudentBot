# 🤖 Телеграм-бот: Конвертер изображений в PDF

Мощный и эффективный телеграм-бот, разработанный для удобной работы с документами. На данный момент специализируется на конвертации множества изображений в единый, качественный PDF-файл.

## ✨ Ключевые Возможности

- 📸 **Пакетная обработка фото**: Загружайте фотографии по одной или целыми альбомами. Бот интеллектуально собирает их в одну сессию.
- 📄 **Мгновенное создание PDF**: Объединяет все собранные изображения в профессиональный PDF-документ за считанные секунды.
- ✏️ **Умная система именования**:
  - **Авто-дата**: Автоматическая генерация имен, например `PDF_20231025_1430.pdf`.
  - **Своё название**: Возможность задать (ввести вручную) понятное имя для файла.
- 📊 **Логирование активности**: Автоматически отслеживает взаимодействия пользователей и статистику конвертаций в базе данных PostgreSQL.
- 🧹 **Авто-очистка**: Временные файлы удаляются автоматически после обработки, поддерживая чистоту на сервере.
- 🚀 **Высокая производительность**: Построен на `asyncio` и `aiogram 3.x` для быстрой, асинхронной работы.

## 🛠 Технологический Стек

- **Язык**: Python 3.10+
- **Фреймворк**: [Aiogram 3.x] (Асинхронный)
- **База данных**: PostgreSQL 15
- **ORM**: SQLAlchemy (Async)
- **Контейнеризация**: Docker и Docker Compose (для БД)
- **Утилиты**: `img2pdf` (Высокопроизводительная конвертация), `pydantic-settings`

## 🚀 Установка и Запуск

Следуйте этим шагам, чтобы развернуть бота на локальной машине или сервере.

### 1. Клонирование репозитория
```bash
git clone https://github.com/Kasper215/StudentBot.git
cd StudentBot
```

### 2. Настройка окружения
Создайте файл `.env` в корневой директории проекта:
```env
BOT_TOKEN=ваш_токен_бота_здесь
PG_URL=postgresql+asyncpg://student_user:student_password@localhost:5432/student_bot_db
```

### 3. Запуск базы данных (Docker)
Убедитесь, что Docker запущен, затем поднимите контейнер с базой данных:
```bash
docker-compose up -d
```

### 4. Установка зависимостей
Создайте виртуальное окружение и установите необходимые библиотеки:
```bash
# Windows
python -m venv .venv
.venv\Scripts\activate

# Linux/macOS
python3 -m venv .venv
source .venv/bin/activate

# Установка пакетов
pip install -r requirements.txt
```

### 5. Запуск бота
```bash
python main.py
```

## 📖 Руководство пользователя

1. **Старт**: Отправьте команду `/start` для начала работы.
2. **Загрузка**: Отправляйте фотографии (как фото, не как файлы) в чат. Можно отправлять их группой (альбомом).
3. **Финиш**: Когда все фото загружены, нажмите кнопку **"✅ ГОТОВО, СОБРАТЬ PDF"**, прикрепленную к последнему сообщению с фото.
4. **Имя файла**: Выберите: использовать текущую дату/время или ввести своё название.
5. **Скачивание**: Получите готовый PDF-файл мгновенно!

## 📂 Структура Проекта

```
tgbot/
├── database/           # Модели базы данных и логика подключения (SQLAlchemy)
├── handlers/           # Обработчики сообщений (в данный момент интегрированы в main.py)
├── services/           # Бизнес-логика (конвертация PDF, очистка файлов)
├── temp/               # Временное хранилище пользовательских файлов (авто-очищаемое)
├── .env                # Переменные окружения (вкл. секреты)
├── docker-compose.yml  # Конфигурация Docker для PostgreSQL
├── main.py             # Точка входа и логика бота
└── requirements.txt    # Зависимости Python
```


---
*Created with ❤️ by Kasper*
